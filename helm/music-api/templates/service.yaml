apiVersion: v1
kind: Service
metadata:
  name: {{ include "music-api.fullname" . }}
  labels:
    {{- include "music-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "music-api.selectorLabels" . | nindent 4 }}
